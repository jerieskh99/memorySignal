# Local run state written by the console bridge / subset_run at runtime.
runs/
__pycache__/
*.pyc

# configs/ (saved plans) is intentionally NOT ignored -- those are meant to
# persist in the repo and be version-controlled.
