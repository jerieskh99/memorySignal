# Local run state written by console_bridge.py and subset_run.py at runtime --
# per-run metadata, generated steps files, capture logs, and staging temp files.
# These are machine/run-specific, not source.
runs/

# Python caches
__pycache__/
*.pyc
